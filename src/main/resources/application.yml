server:
  port: 9091
spring:
  application:
    name: qdbpostsandcomments

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8010/eureka